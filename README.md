# 🧠 MNIST Digit Recognizer using CNN & Gradio

This project implements a **Handwritten Digit Recognition system** using a **Convolutional Neural Network (CNN)** trained on the **MNIST dataset**, with an interactive **Gradio web interface** for predictions.

---

## 📌 Project Features

- Train a CNN model on the MNIST dataset
- Save & load the trained model automatically
- Upload an image of a handwritten digit
- Automatic preprocessing (resize, normalization, color inversion)
- Predict digit with confidence score
- Interactive GUI using **Gradio**

---

## 🛠 Technologies Used

- Python 3
- TensorFlow / Keras
- NumPy
- Matplotlib
- Gradio
- Pillow (PIL)

---

## 📂 Project Structure

